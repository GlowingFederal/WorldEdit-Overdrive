package com.glowingfederal.worldeditoverdrive.execution;

/** Reserved ownership seam; Stage 5A uses the existing per-chunk lighting and packet path. */
public enum FinalizationIntent { CHANGED_CHUNKS_ONCE }
